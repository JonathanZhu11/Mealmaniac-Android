<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MealPreviewActivity" >
    
   <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:weightSum="2"
        android:orientation="vertical"
        >
        
		<android.support.v4.view.ViewPager
			    android:id="@+id/pager"
			    android:layout_width="wrap_content"
			    android:layout_height="0dp"
			    android:layout_weight="1"
			    android:background="@color/gray">
			    
		</android.support.v4.view.ViewPager>
    </LinearLayout>
    
	<Button
        android:id="@+id/background_button"
        style="@style/AppBaseTheme"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="36dp"
        android:background="@drawable/circle_white"
        android:textColor="@color/White" />
	
    <Button
        android:id="@+id/finalize_button"
        style="@style/AppBaseTheme"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignBaseline="@+id/background_button"
        android:layout_alignBottom="@+id/background_button"
        android:layout_alignLeft="@+id/background_button"
        android:layout_marginLeft="10dp"
        android:background="@drawable/circle"
        android:text="@string/finalize_button"
        android:textColor="@color/White" 
        android:textSize="15sp"/>

    

</RelativeLayout>